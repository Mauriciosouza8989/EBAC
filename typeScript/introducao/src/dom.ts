const campoNome = document.getElementById('#campoNome');
const formulario = document.getElementById('formulario');
const buttonEnviar = document.getElementById('#button');

buttonEnviar?.addEventListener('click', (e:MouseEvent) =>{
});